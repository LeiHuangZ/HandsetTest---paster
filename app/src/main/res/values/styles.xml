<resources>

    <!-- Base application theme. -->

    <!-- Full Screen theme -->
    <style name="FulScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
